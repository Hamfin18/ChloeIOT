<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chloeiot">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TargetHelp"></activity>
        <activity android:name=".HelpActivity" />
        <activity android:name=".AppInfoActivity" />
        <activity android:name=".TargetStory" />
        <activity android:name=".StoryActivity" />
        <activity
            android:name=".NotificationWatering"
            android:label="Notification" />
        <activity
            android:name=".AboutActivity"
            android:label="About" />
        <activity android:name=".TaskActivity" />
        <activity
            android:name=".HomeActivity"
            android:label="Home" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>